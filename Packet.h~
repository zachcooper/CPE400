#ifndef PACKET_H
#define PACKET_H

#include "WeightedGraph.h"

#include <ostream>

struct Packet{
	int energyConsumptionRequired;
	WeightedGraph::Vertex* source;//source;
	WeightedGraph::Vertex* dest;//destination;
};


#endif
